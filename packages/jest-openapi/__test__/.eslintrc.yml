env:
  jest/globals: true

plugins:
  - jest

extends:
  - plugin:jest/all

rules:
  jest/prefer-expect-assertions: off
  jest/no-disabled-tests: warn
  jest/prefer-lowercase-title: error
  jest/no-hooks: off
